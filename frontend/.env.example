# Optional. Used when building for production (e.g. Render/Vercel).
# Set this in your hosting dashboard so the built app calls your backend, not localhost.
# Example for Render backend:
# VITE_API_URL=https://windsmit-backend.onrender.com/api
