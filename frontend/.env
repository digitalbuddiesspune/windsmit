# Backend API URL — one .env for local and production

# Local development
VITE_BACKEND_URL=http://localhost:5001
VITE_API_URL=http://localhost:5001/api

# Production (Render backend) — used when building for deploy
VITE_API_URL_PRODUCTION=https://windsmit-backend.onrender.com/api
